/**
 * InputController — abstração de input.
 *
 * Responsabilidade: traduzir o D-pad e os botões virtuais (pular, corpo a corpo, à
 * distância, defender) em intenções de alto nível consumidas pela
 * PlayerStateMachine, isolando o restante do jogo da origem do input.
 *
 * Referência: System Design §14 (Controles virtuais); UI em src/ui/VirtualControls.ts.
 */
