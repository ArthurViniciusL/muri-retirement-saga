/**
 * MemoryPuzzleEngine — regras do jogo da memória.
 *
 * Responsabilidade: embaralhar e distribuir as 25 cartas (5x5) do tema recebido,
 * validar os pares virados, contar o limite de 2 minutos e decidir entre conclusão
 * (>= 3 pares) e reinício em loop. Lógica pura, desacoplada da renderização feita
 * pela PuzzleScene.
 *
 * Referência: System Design §11 (Puzzle — jogo da memória).
 */
