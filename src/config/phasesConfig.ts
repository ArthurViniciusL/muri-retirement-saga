/**
 * phasesConfig — configuração data-driven das três fases.
 *
 * Responsabilidade: declarar a interface PhaseConfig (id, tilemapKey, enemySpawns,
 * thiefEncounters, puzzleThemeKey, puzzleTriggerZone, essentialItem) e os objetos
 * das Fases 1, 2 e 3, para que ajustes de conteúdo não exijam tocar na lógica das
 * cenas.
 *
 * Referência: System Design §13 (Estrutura de dados das fases), §20 (Level design).
 */
