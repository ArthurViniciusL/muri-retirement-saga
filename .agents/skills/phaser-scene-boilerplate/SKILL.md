---
name: phaser-scene-boilerplate
description: Gera o boilerplate de uma nova cena Phaser 3 do jogo Muricarliton seguindo os padrões do System Design — assinatura da classe, chave da cena, ciclo preload/create/update e o padrão de overlay com scene.launch/scene.pause quando a cena for sobreposta. Use ao criar qualquer cena nova em src/scenes/.
---

# phaser-scene-boilerplate

> ⚠️ Stub. As instruções completas da skill serão escritas em etapa posterior.

Gera a estrutura padrão de uma cena Phaser conforme a arquitetura de cenas definida
no System Design (§3), incluindo a convenção de cenas sobrepostas (`scene.launch` +
`scene.pause`) usada pela `PuzzleScene`.

**Documentos de referência:** `.agents/docs/system-design.md` (§3, §4, §13).
